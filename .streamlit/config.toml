[server]
# Force wide mode for better data display
layout = "wide"

[theme]
# Use a clean light theme that works well for data/tables
primaryColor = "#FF4B4B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
font = "sans serif"